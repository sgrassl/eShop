---
applyTo: "**/*.cs"
---

# C# Usage
- Always generate an empty default constructor for all classes you touch.
- Use expression-bodied members for simple property getters and methods.
- Use `var` for local variable declarations when the type is obvious from the right-hand side.
- Prefer `async` and `await` for asynchronous programming.
- Use `using` statements for resource management.